
if [[ -d "${rvm_path:-$HOME/.rvm}/environments" \
  && -s "${rvm_path:-$HOME/.rvm}/environments/ruby-1.9.3-p194@linker" ]] ; then
  \. "${rvm_path:-$HOME/.rvm}/environments/ruby-1.9.3-p194@linker"
else
  rvm --create use  "ruby-1.9.3-p194@linker"
fi
