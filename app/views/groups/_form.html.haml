= simple_form_for(@group) do |f|
  %p
    = f.label :group_name
    %br/
    = f.text_field :group_name, options = {:autofocus => true, :required => true}
  %p
    = f.label :group_description
    %br/
    = f.text_field :group_description
  %p
    = f.submit "Save"
