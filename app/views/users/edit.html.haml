%h3 Editing user
= form_for(@user) do |f|
  -if @user.errors.any?
    %div#error_explanation
      %h2
        =pluralize(@user.errors.count, "error")+' '
        prohibited this user from being saved:
      %ul
      -@user.errors.full_messages.each do |msg|
        %li
          =msg
  %p
    = f.label :username, options = {:autofocus => true}
    %br/
    = f.text_field :username, options = {:required => true}
  = render 'admin_section', :f => f
  %p
    = f.submit "Update"
= link_to 'Show', @user
|
\#{link_to 'Back', users_path}
